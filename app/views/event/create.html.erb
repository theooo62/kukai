<!-- eventのindex -->

<class="new-event">
    <a class="btn" href="/event">新規句会作成</a>
  </div>


  <h1>句会を作成する</h1>

  <div class="kukai-form">
      <!-- ここからフォーム -->
      <% if current_user.id %>
        <%= form_tag('/event' , method: :post) do %>
          <textarea cols="100" event_title="text" placeholder="句会名" rows="2"></textarea>
          <input type="submit" value="SENT">
        <% end %>
        <% end %>
    </div>
